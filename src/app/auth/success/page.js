"use client"

import React from 'react'

// useEffect(() => {
//   const params = new URLSearchParams(window.location.search);
//   const token = params.get("token");

//   if (token) {
//     document.cookie = `JWT_TOKEN=${token}; path=/; secure; samesite=None`;
//     window.location.href = "/"; 
//   }
// }, []);

const page = () => {

  return (
    <div>page</div>
  )
}

export default page