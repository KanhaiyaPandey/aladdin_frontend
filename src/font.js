import localFont from "next/font/local";

export const slussen = localFont({
  src: "../public/fonts/Slussen-Regular-TRIAL.otf",
  variable: "--font-slussen",
  display: "swap",
});

export const michroma = localFont({
  src: "../public/fonts/Michroma.ttf",
  variable: "--font-michroma",
  display: "swap",
});
